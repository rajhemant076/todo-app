# Frontend environment variables (Vite)
# Vite proxies /api → backend, so you normally don't need this.
# Only set this if you deploy frontend and backend on different origins.
VITE_API_BASE_URL=http://localhost:5000
